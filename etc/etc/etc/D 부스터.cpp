#pragma warning(disable : 4996)
#include<cstdio>
#include<queue>
#include<unordered_map>
using namespace std;

struct data {
	int x, y;
}arr[250001];

int N, Q;
void BFS()
{

}
int main()
{
	scanf("%d %d", &N, &Q);
	for (int i = 1; i <= N; ++i)scanf("%d %d", &arr[i].x, &arr[i].y);
	for (int i = 1; i <= Q; ++i) {
		int a, b, x;
		scanf("%d %d %d", &a, &b, &x);
	}

}